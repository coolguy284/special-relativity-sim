export let velMag = 0, velAng = 0;

function setVelMag(newVelMag) {
  velMag = newVelMag;
}

function setVelAng(newVelAng) {
  velAng = newVelAng;
}

export let velLorenzFactor = 1;

function setVelLorenzFactor(newVelLorenzFactor) {
  velLorenzFactor = newVelLorenzFactor;
}

export let velRelativityScaleFactor = 1;

function setVelRelativityScaleFactor(newVelRelativityScaleFactor) {
  velRelativityScaleFactor = newVelRelativityScaleFactor;
}

export let velMagAdj = 0;

function setVelMagAdj(newVelMagAdj) {
  velMagAdj = newVelMagAdj;
}

export let accMag = 0;

function setAccMag(newAccMag) {
  accMag = newAccMag;
}

export let accAng = 0;

function setAccAng(newAccAng) {
  accAng = newAccAng;
}
